apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: lmsapp          # ← must match pod namespace
type: Opaque
data:
  POSTGRES_DATASOURCE_PASSWORD: c3VwZXJzZWNyZXQ=  # base64 for "supersecret"
  